### foodorderingapp

#steps to initalize backend
1. clone the github repo
2. npm init for configur package json file
3. install dependencies by npm i express mongoose dotenv cors

server is ready to start 
